from setuptools import find_packages, setup

setup(
        name = "gracefall",
        packages=find_packages(),
        description="cpsc547 viz project",
        author="Ian Hill, Matthew Tang",
        version="0.0.0"
)
